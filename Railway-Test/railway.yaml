services:
  - type: web
    name: attendance-app
    env: docker
    dockerfilePath: Railway-Test/Dockerfile
    autoDeploy: true
    envVars:
      - key: CHROME_BIN
        value: /usr/bin/chromium
      - key: CHROMEDRIVER_PATH
        value: /usr/bin/chromedriver
